import { Container } from 'react-bootstrap';

//components
import TableForm from '../features/TableForm';

const Table = () => {
  return (
    <Container>
      <TableForm />
    </Container>
  );
};

export default Table;
